function suma(){
    var num1;
    var num2;
    var resultado = parseInt(num1)+parseInt(num2);
    var numeroMostrado = document.getElementById("numero").dataset.num;
    console.log(numeroMostrado);
}